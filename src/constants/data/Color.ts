export enum Color {
  info = "info",
  success = "success",
  warning = "warning",
  danger = "danger",
  primary = "primary",
  default = "default",
  secondary = "secondary",
  accent = "accent",
}
